#include<stdio.h>
#include<conio.h>
main()
{
  clrscr();
  int x,formula;
  int y;

  printf("ente value ofx=",x);
  scanf("%d",&x);

  printf("enter value of y=",y);
  scanf("%d",&y);

  formula=(x*x)+(2*x*y)+(y*y);
  printf("(x+y)^2=%d",formula);

  getch();
  }

